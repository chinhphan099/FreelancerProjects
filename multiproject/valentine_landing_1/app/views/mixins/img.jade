mixin img(src, alt)
  img(src=src ? 'images/' + src : 'images/transparent.png', alt!="#{alt}")&attributes(attributes)

mixin img-upload(src, alt)
  img(src=src ? 'images/upload/' + src : 'images/transparent.png', alt!="#{alt}")&attributes(attributes)
